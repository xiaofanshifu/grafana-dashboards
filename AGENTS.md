# AGENTS.md

## 最高原则
- 用户明确声明为只读的文件/目录，禁止任何写操作：覆盖、删除、重命名、修改属性/时间戳、git 恢复等。
- 未经用户明确指示，当前工作目录不执行文件写操作、删除、git 状态变更。
- 指令范围不明确时，先向用户确认，得到答复后再执行。

## 文件操作
- 用户要求修复、实现、重构、删除某文件时，视为授权对相关文件进行必要写操作；但操作前应先说明将修改哪些文件，得到确认后执行。
- 临时文件、缓存、日志、调试输出，写入系统临时目录（如windows是 `%TEMP%`，即 `C:\Users\<用户名>\AppData\Local\Temp`），不要污染项目目录。
- 禁止在项目目录中创建无关文件/目录。
- 不执行 `git restore`、`git checkout`、`git reset` 等可能丢失工作的命令，除非用户明确要求并确认。
- 不擅自执行 `git add`、`git commit`、`git push` 等改变仓库状态的命令。

## 搜索
- 联网搜索优先参考官方文档，第三方文档和博客仅作参考。
- 本机有代理，直连不可达时可尝试通过 `127.0.0.1:7890` 访问目标站点，仅用于读取，不用于上传敏感信息。

## 报告
- 每次写操作、删除、git 状态变更或运行外部命令后，简要报告：修改/创建/删除的文件、执行的命令、结果。